{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm build && SERVER_DIR=$(find .next/standalone -name 'server.js' -not -path '*/node_modules/*' -exec dirname {} \\;) && cp -r public \"$SERVER_DIR/public\" && cp -r .next/static \"$SERVER_DIR/.next/static\" && mkdir -p \"$SERVER_DIR/data\""
  },
  "deploy": {
    "startCommand": "cd $(find .next/standalone -name 'server.js' -not -path '*/node_modules/*' -exec dirname {} \\;) && node server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
